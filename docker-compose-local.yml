version: '3.9'

services:
  server:
    build:
      context: .
      dockerfile: local.Dockerfile
    image: ppu-app-local:latest
    volumes:
      - ./src:/app/src
    container_name: ppu-container
    ports:
      - "8080:8080"
    restart: always
    env_file:
      - ./.env